# 前端访问后端的基址（构建时注入到 process.env.VUE_APP_API_BASE）。
# 开发默认 http://localhost:9999（与 src/utils/request.js 的回退一致）。
# 部署/换域名时：复制本文件为 .env.production 并改成你的后端地址。
#   同域反代时填 /api；跨域直连则填完整地址如 https://api.yourdomain.com
VUE_APP_API_BASE=http://localhost:9999
