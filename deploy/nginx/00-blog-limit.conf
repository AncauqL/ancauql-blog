# 登录/注册限流区域（http 级配置，放到 /etc/nginx/conf.d/00-blog-limit.conf）
# 10m 约可存 16 万个 IP；按 IP 统计

limit_req_zone $binary_remote_addr zone=blog_login:10m rate=10r/m;

# 反代公共头（被 blog-site.conf 的 location include）
# 若不想用 include，可把下面几行直接内联到各 location。
