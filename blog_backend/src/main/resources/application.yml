server:
  port: 9999

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: "${DB_URL:jdbc:mysql://localhost:3306/blog_system?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true}"
    username: "${DB_USERNAME:root}"
    # 不再提供默认密码：必须由环境变量提供（本机 dev-env.bat / 服务器 EnvironmentFile）
    password: "${DB_PASSWORD:}"
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 12MB

blog:
  # 图片上传保存目录（相对路径基于后端进程工作目录）
  upload-dir: ./uploads
  # RSS 订阅源信息
  site-title: AncauqL 的个人博客
  # 前台站点基址（feed 文章链接会拼它）；换域名用 BLOG_SITE_URL 覆盖
  site-url: "${BLOG_SITE_URL:http://localhost:8081}"
  # 允许跨域的前端来源（逗号分隔）；生产同域反代时可留空
  cors-allowed-origins: "${CORS_ALLOWED_ORIGINS:http://localhost:8080,http://localhost:8081}"

mybatis-plus:
  mapper-locations: classpath:mappers/*.xml
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl



















